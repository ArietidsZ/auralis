# 生产级继续执行授权与验收约束

2026-09-06，用户明确授予项目全权，可推翻原有设计与实现；Pi GLM 尽量使用 max，通过并行提速，Pi Grok 也可使用。此授权优先于旧计划中保留目录、暂不下载模型等默认取舍。

目标为生产级可用的离线跨语言/方言语音传译，性能和准确度不得显著落后于同任务、同硬件、同资源与隐私约束下的领先实现。

当前并行：GLM max 负责真实 MT native/Android 构建和 TTS 真实执行；Grok xhigh 负责 ASR 独立审计、长音频修复与移动接入。主审负责依赖、接口、跨端编译和全量验收。

比较必须固定模型与runtime revision、数据集及归一化规则、线程/内存预算、硬件和热状态，记录所有样本及失败。不删困难样本来抬高数字；桌面、模拟器、真机结果分开。模型论文指标不能充当本应用实测；准确率与延迟/内存同时比较。

先完成真实推理与可复现构建，然后逐项消融：默认上游实现、量化精度、线程数、分段/流式策略、不同EP和共享runtime。仅保留有正确性或实测收益的复杂度。完整设备指标缺数据时继续标未验证。

固定产物缓存：/Users/arietids/Library/Caches/Auralis（临时目录在暂停期间已被清理，已按固定哈希恢复）；NDK r27d 已在实际SDK目录安装并运行clang，官方dmg SHA1已验证。本机RAM32GiB，无已连接Android设备。
